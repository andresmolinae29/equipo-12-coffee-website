<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="UTF-8">
	<title>Editando producto</title>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<script src="https://kit.fontawesome.com/5cc45362ff.js" crossorigin="anonymous"></script>
	<link href="https://fonts.googleapis.com/css2?family=Lato:wght@100;300&family=Roboto:wght@100;300;400&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="/css/editStyle.css">
	
</head>
<body>
	<%- include('./partials/header') %>
	<!-- /Header -->

	<div class="divEditForm">
		<div class="row">
			<di>
				<h2 class="product-title">Estás editando: <i><%= productToEdit.name %></i></h2>
			</div>
		</div>
		<div>
			<form action="/product/edit/<%= productToEdit.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
				<div class="row product-detail">
					<div>
						<label for="name" class="form-label">Nombre del producto:</label>
						<input type="text" id="name" name="name" value="<%= productToEdit.name %>" placeholder="Ej: Cafetera Hario" class="form-input">
					</div>
					<div>
						<label for="price" class="form-label">Precio del producto:</label>
						<input type="text" id="price" name="price" value="<%= productToEdit.price %>" placeholder="Ej: 123" class="form-input">
					</div>
					<div>
						<label for="discount" class="form-label">Descuento:</label>
						<input type="text" id="discount" name="discount" value="<%= productToEdit.discount %>" placeholder="Ej: 15" class="form-input">
					</div>
					<div>
						<label for="category" class="form-label">Categoría:</label>
						<select name="category" id="category" class="form-input">
							<option value="">Elegí</option>
							<option value="in-sale" <%= (productToEdit.category == "in-sale")? "selected":"" %> >En oferta</option>
							<option value="visited" <%= (productToEdit.category == "visited")? "selected":"" %> >Últimos agregados</option>
						</select>
					</div>
					<div>
						<label for="description" class="form-label">Descipción:</label>
						<textarea name="description" id="description" class="form-input"><%= productToEdit.description %></textarea>
					</div>
					<div>
						<label for="img" class="btn-save foto">Foto del producto</label>
						<br><input type="file" id="img" name="img" class="form-input">
					</div>
					<div>
						<button type="submit" class="buy-now-button">Guardar producto</button>
					</div>
				</div>
			</form>
		</div>

	</div>
	<!-- footer -->
	<%- include('./partials/footer') %>
</body>	
</html>